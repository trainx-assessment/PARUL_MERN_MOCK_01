const form=document.getElementsById("#studentForm")
const data=document.getElementsByClassName(".Form_structure")
const submission=document.getElementsByClassName(".Buttons_Section")
submission.addEventListener("submit",(event)=>{
    submission.textContent="Submitted"
})
